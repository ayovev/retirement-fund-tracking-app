{
  "dependencies": {
    "dotenv": "^6.0.0",
    "express": "^4.16.2",
    "jwt-simple": "^0.5.5",
    "moment": "^2.22.2",
    "mongodb": "^3.1.8",
    "morgan": "^1.9.0",
    "request": "^2.87.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "nodemon": "^1.18.2"
  },
  "scripts": {
    "lint": "eslint --fix ./src/*.js",
    "start": "cd src && nodemon --inspect server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
